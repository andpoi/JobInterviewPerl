#!/usr/bin/perl
#Andrew Poirier
#Exercise 3 for ThinkGeek
print "Please input a string to check if it starts with A-Z\n";
$input = <>;
if ($input =~ /^[A-Z]/) #Matches the pattern A-Z anchored to the beginning of the input string
	{ print "The string starts with A-Z\n" }
	else{ print "The string doesn't start with A-Z\n" }
